import { Room } from "@microsoft/microsoft-graph-types";
import { graphGet, GraphQueryable, SPFx } from "@pnp/graph";

export interface IRoomInfoProvider {
    getRoomInformation(): any;
}




export class RoomInfoProvider  {
    constructor(parameters) {
        
    }
 
}